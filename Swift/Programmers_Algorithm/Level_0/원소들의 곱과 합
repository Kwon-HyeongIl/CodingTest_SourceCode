import Foundation

func solution(_ num_list:[Int]) -> Int {
    
    var mul = 1
    var sum = 0
    
    for n in num_list {
        mul *= n
        sum += n
    }
    
    return mul < sum*sum ? 1 : 0
}
