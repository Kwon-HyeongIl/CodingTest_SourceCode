import Foundation

func solution(_ a:Int, _ b:Int) -> Int {
    
    if Int(String(a)+String(b))! > 2*a*b {
        return Int(String(a)+String(b))!
    } else if Int(String(a)+String(b))! < 2*a*b {
        return 2*a*b
    } else {
        return Int(String(a)+String(b))!
    }
}
