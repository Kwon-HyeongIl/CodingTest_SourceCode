class Solution {
    boolean solution(String s) {
        s = s.toLowerCase();
        
        int pL = s.length() - s.replace("p", "").length();
        int pY = s.length() - s.replace("y", "").length();
        
        return pL == pY;
    }
}
