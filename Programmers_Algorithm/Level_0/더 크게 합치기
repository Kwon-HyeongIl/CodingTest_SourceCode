class Solution {
    public int solution(int a, int b) {
        int one = Integer.valueOf(a+""+b);
        int two = Integer.valueOf(b+""+a);
        
        if (one > two) {
            return one;
        } else if (one < two) {
            return two;
        } else {
            return one;
        }

    }
}
