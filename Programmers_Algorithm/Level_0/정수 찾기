import java.util.*;

class Solution {
    public int solution(int[] num_list, int n) {
        
        List<Integer> list = new ArrayList<>();
        Arrays.stream(num_list).forEach(list::add);
        
        if (list.contains(n)) {
            return 1;
        } else {
            return 0;
        }
    }
}
