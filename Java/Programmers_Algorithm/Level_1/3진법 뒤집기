class Solution {
    public int solution(int n) {
        String num3 = Integer.toString(n, 3); // 숫자를 3진수로 변환하는 메소드
        String reversed = new StringBuilder(num3).reverse().toString();
        return Integer.valueOf(reversed, 3); // 문자열을 10진수로 변환하는 메소드
    }
}
