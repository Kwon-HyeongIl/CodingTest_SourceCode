class Solution {
    public int solution(String my_string) {
        
        int sum = 0;
        
        for (char c : my_string.toCharArray()) {
            if (c>='0' && c<='9') {
                sum += Character.getNumericValue(c);
            }
        }
        
        return sum;
    }
}
