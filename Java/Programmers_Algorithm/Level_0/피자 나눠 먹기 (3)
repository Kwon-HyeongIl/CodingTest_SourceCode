class Solution {
    public int solution(int slice, int n) {
        
        int temp = n/slice;
        
        if (n%slice > 0) {
            temp++;
        }
        
        return temp;
    }
}
